What’s New in C# 14? Key Features and Updates
1) Extension Members - Add instance-like methods, properties, operators, and static members to existing types.
2) Null-Conditional Assignment (??= extended scenarios) - Assign only if null, now improved in more contexts.
3) nameof Supports Unbound Generic Types - Can use nameof(List<>), nameof(Dictionary<,>), etc.
4) More Implicit Conversions for Span<T> / ReadOnlySpan<T> - Spans can be created more naturally.
5) Modifiers on Simple Lambda Parameters - Use ref, out, in, scoped in lambdas.
6) Field-Backed Properties - Compiler auto-creates a backing field you can access.
7) Partial Events and Constructors - Split constructors and events across partial classes.
8) User-Defined Compound Assignment Operators - Define operators like +=, -=, *=, etc.
